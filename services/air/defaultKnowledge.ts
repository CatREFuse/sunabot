export const DEFAULT_AIR_KNOWLEDGE = `# 场域知识

## 使用边界

- 每条约定都写明适用的群、私聊、小团体或参与者，不外推到其他场域。
- 只保留已经明确确认的称呼、边界、规则、前提、例外和纠正；证据不足时不写入。
- 不记录密码、令牌、住址、身份证件、财务凭据等秘密，也不推断健康、政治立场、性取向等敏感属性。

## 场域约定

当前没有已确认的场域约定。
`;
