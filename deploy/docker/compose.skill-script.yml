services:
  skill-script-image:
    image: ${SUNABOT_SKILL_SCRIPT_IMAGE:-sunabot-skill-script:local}
    profiles: ["build"]
    build:
      context: .
      dockerfile: Dockerfile.skill-script
    network_mode: none
    read_only: true
    entrypoint: ["/usr/bin/true"]
