interface:
  display_name: "Workbench 配置"
  short_description: "管理 Bot 的表情、Skills 与双 Workbench 资源"
  default_prompt: "Use $workbench-config when the Bot needs to add emojis or Skills, inspect Workbench resources, or resolve Native and Docker Workbench paths."
policy:
  allow_implicit_invocation: true
